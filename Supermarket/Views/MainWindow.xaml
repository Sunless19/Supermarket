<Window x:Class="Supermarket.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Supermarket.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="454" Width="700" WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <local:MainWindowVM />
    </Window.DataContext>
    <Window.Background>
        <ImageBrush ImageSource="/Resources/SUPERMARKET (3).png"/>
    </Window.Background>
    <Grid Margin="0,0,0,-6">
        <Grid.Background>
            <ImageBrush ImageSource="/Resources/SUPERMARKET (3).png"/>
        </Grid.Background>
        <Button Content="Start" HorizontalAlignment="Center" Margin="0,247,0,0" VerticalAlignment="Top" Height="43" Width="242" Background="{x:Null}" BorderBrush="#FF27B52F" FontFamily="Bradley Hand ITC" FontSize="24" Command="{Binding StartCommand}"/>
    </Grid>
</Window>
